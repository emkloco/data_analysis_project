def test_infrastructure_connection():
    """
    CRITICAL: This test confirms that CircleCI is connected 
    and can run Python code. If this passes, the pipeline works.
    """
    assert 1 + 1 == 2